@Library('sprints') _

pipeline {
  agent any
  
  stages {
    stage('Execute Groovy Script') {
      steps {
        myScript()
      }
    }
  }
  stages {
    stage('Executes') {
      steps {
        myScript()
      }
    }
  }
  stages {
    stage('Ex') {
      steps {
        myScript()
      }
    }
  }
}
