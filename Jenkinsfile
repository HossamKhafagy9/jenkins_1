@Library('sprints') _

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    pipeline()
                }
            }
        }
    }
}
